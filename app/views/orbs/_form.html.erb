<%= form_for(@orb) do |f| %>
  <% if @orb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@orb.errors.count, "error") %> prohibited this orb from being saved:</h2>

      <ul>
      <% @orb.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <% Book.all.each do |book| %>
    <%= check_box_tag "orb[book_ids][]", book.id, @orb.books.include?(book) %>
    <%= book.name %>
    <%end%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
